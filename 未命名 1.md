你这次判断是对的。**如果目标是完整移植 FastGS 到 DualGS，首帧 joint 阶段就应该把 rasterizer 接口做成最终形态**，不能靠 dummy 输出临时糊过去。否则后面 skin / 后帧一旦用到 DualGS 原 rasterizer 的扩展输出，就还得返工。

下面先只写：

# 1. 首帧 joint 训练

目标：  
在 `training_joint(..., is_start_frame=True)` 下，让 DualGS 使用 **合并后的 FastGS-style rasterizer + FastGS densify/prune + FastGS optimizer 策略** 跑通首帧 JointGS。

核心原则：

```text
以 DualGS 的 rasterizer 为主体；
把 FastGS 的 compact tile、metric_map、accum_metric_counts 合并进去；
保留 DualGS 原有 counts/lists/accum_weights/accum_dist 等输出；
不要用 dummy 输出；
不要直接用 FastGS rasterizer 替换 DualGS rasterizer。
```

---

## 1.1 移植 rasterizer

这是第一阶段最重要的部分。  
正确做法不是新增一个 `diff_gaussian_rasterization_fastgs` 然后绕接口，而是：

```text
在 DualGS/submodules/diff-gaussian-rasterization/ 里面合并 FastGS 的 CUDA 逻辑。
```

最终目标接口应该是 DualGS + FastGS 的并集：

```python
rendered_image,
radii,
counts,
lists,
listsRender,
listsDistance,
centers,
depths,
my_radii,
accum_weights,
accum_count,
accum_blend,
accum_dist,
accum_metric_counts
```

也就是说，DualGS 原来的输出全部保留，FastGS 新增的 `accum_metric_counts` 追加进去。

---

### 1.1.1 修改 Python rasterizer 封装接口

文件：

```text
DualGS/submodules/diff-gaussian-rasterization/diff_gaussian_rasterization/__init__.py
```

需要处理的内容：

```text
1. GaussianRasterizationSettings 增加 FastGS 参数：
   - mult
   - get_flag
   - metric_map

2. 保留 DualGS 参数：
   - pixel_weights

3. _RasterizeGaussians.forward(...) 里面传参改成并集：
   - bg
   - means3D
   - colors_precomp
   - opacities
   - scales
   - rotations
   - scale_modifier
   - cov3Ds_precomp
   - metric_map
   - viewmatrix
   - projmatrix
   - tanfovx
   - tanfovy
   - image_height
   - image_width
   - dc
   - sh
   - sh_degree
   - campos
   - mult
   - prefiltered
   - debug
   - get_flag
   - pixel_weights

4. forward 返回值从 DualGS 原来的 13 个扩展输出，改成 14 个：
   - 原 DualGS 输出全部保留
   - 追加 accum_metric_counts

5. backward 的函数签名要多接一个 accum_metric_counts 对应的梯度占位参数。
   不需要真的用它反传，但签名必须匹配。
```

注意：  
FastGS 的 `means2D` 是 `[P, 4]`，DualGS 原来是 `[P, 3]`。为了支持 FastGS 的 `xyz_gradient_accum_abs`，这里要接受 `[P, 4]` 的 `means2D` 梯度。

---

### 1.1.2 修改 C++ binding 层

文件：

```text
DualGS/submodules/diff-gaussian-rasterization/rasterize_points.h
DualGS/submodules/diff-gaussian-rasterization/rasterize_points.cu
DualGS/submodules/diff-gaussian-rasterization/ext.cpp
```

其中 `ext.cpp` 基本不用改，因为它只是绑定：

```cpp
m.def("rasterize_gaussians", &RasterizeGaussiansCUDA);
m.def("rasterize_gaussians_backward", &RasterizeGaussiansBackwardCUDA);
```

真正要改的是 `rasterize_points.h/cu`。

需要合并的内容：

```text
1. RasterizeGaussiansCUDA(...) 增加 FastGS 参数：
   - metric_map
   - mult
   - get_flag

2. 保留 DualGS 参数：
   - pixel_weights

3. 新增输出 tensor：
   - metricCount / accum_metric_counts

4. forward 调用 CudaRasterizer::Rasterizer::forward(...) 时传入：
   - metric_map
   - mult
   - get_flag
   - metricCount ptr
   - pixel_weights ptr
   - accum_weights ptr
   - reverse_count ptr
   - blending_weights ptr
   - dist_accum ptr

5. backward 里面的 dL_dmeans2D 从 DualGS 的 `{P, 3}` 改成 FastGS 的 `{P, 4}`。
```

重点：  
`rasterize_points.cu` 是 Python 和 CUDA kernel 的中间层，必须保证参数顺序和 `__init__.py` 完全一致。

---

### 1.1.3 修改 CUDA rasterizer 主接口

文件：

```text
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.cu
```

要做的事：

```text
1. Rasterizer::forward(...) 增加 FastGS 参数：
   - const int* metric_map
   - float mult
   - bool get_flag
   - int* metricCount

2. 保留 DualGS 参数：
   - listBuffer
   - listBufferRender
   - listBufferDistance
   - pixel_weights
   - accum_weights
   - reverse_count
   - blending_weights
   - dist_accum

3. forward 内部仍然保留 DualGS 的额外 buffer 申请和传递。

4. 把 FastGS 的 compact tile 逻辑合并到 DualGS 的 tile counting 和 duplicateWithKeys 阶段。
```

这里是 rasterizer 合并的核心位置。

---

### 1.1.4 合并 FastGS compact tile 逻辑

文件：

```text
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/auxiliary.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.cu
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.cu
```

需要从 FastGS 合并的核心函数/逻辑：

```text
1. auxiliary.h
   - computeEllipseIntersection(...)
   - processTiles(...)
   - duplicateToTilesTouched(...)

2. forward.cu / preprocessCUDA
   原 DualGS：
      getRect(...)
      tiles_touched = rect area

   改成 FastGS：
      duplicateToTilesTouched(..., mult, ..., nullptr, nullptr)
      tiles_touched = compact tile count

3. rasterizer_impl.cu / duplicateWithKeys
   原 DualGS：
      按矩形 tile 范围遍历

   改成 FastGS：
      使用 duplicateToTilesTouched(...) 生成 key/value

4. 保留 DualGS 的：
   - xy_d
   - depths_d
   - radii_d
   - listBuffer
   - listBufferRender
   - listBufferDistance
```

这一部分不能只复制 FastGS 文件覆盖 DualGS。  
因为 FastGS 没有 DualGS 的这些输出：

```text
counts
lists
listsRender
listsDistance
accum_weights
accum_count
accum_blend
accum_dist
```

正确方式是：**DualGS 为主，FastGS compact tile 逻辑嵌进去。**

---

### 1.1.5 合并 FastGS metric_map / accum_metric_counts 逻辑

文件：

```text
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.cu
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.cu
```

要合并 FastGS 的逻辑：

```text
1. renderCUDA(...) 增加：
   - metric_map
   - metricCount

2. 在每个像素贡献时判断：
   - 如果 get_flag=True
   - 如果 metric_map[pix_id] == 1
   - 对当前 Gaussian 的 metricCount 做 atomicAdd

3. 最终返回 accum_metric_counts。
```

注意：  
DualGS 当前已经在 `renderCUDA` 里处理：

```text
pixel_weights
accum_weights
reverse_count
blending_weights
dist_accum
```

所以这里要把 FastGS 的 metric 统计和 DualGS 的 pixel weight 统计并存，不能二选一。

---

### 1.1.6 编译安装 rasterizer

修改完后重新安装：

```bash
cd DualGS
pip uninstall diff-gaussian-rasterization -y
pip install submodules/diff-gaussian-rasterization
```

验证点：

```text
1. import diff_gaussian_rasterization 成功
2. GaussianRasterizationSettings 能接受：
   - pixel_weights
   - mult
   - get_flag
   - metric_map

3. rasterizer forward 返回 14 项
4. backward 正常
5. dL_dmeans2D 是 [P, 4]
```

---

## 1.2 修改 DualGS 的 Python render 入口

文件：

```text
DualGS/gaussian_renderer/__init__.py
```

目标：  
保留函数名 `render(...)`，不要再额外搞 `render_fastgs_dual(...)`。因为现在底层 rasterizer 已经是合并版，Python 上层就应该统一走一个入口。

需要修改的点：

```text
1. screenspace_points 从 DualGS 原来的：
   torch.zeros_like(pc.get_xyz)   # [P, 3]

   改成：
   torch.zeros((P, 4))            # [P, 4]

2. GaussianRasterizationSettings 增加：
   - mult
   - get_flag
   - metric_map

3. 保留：
   - pixel_weights

4. rasterizer(...) 返回值增加：
   - accum_metric_counts

5. render(...) 返回 dict 里增加：
   - "accum_metric_counts"

6. 原 DualGS 返回的这些 key 全部保留：
   - counts
   - lists
   - listsRender
   - listsDistance
   - gaussian_centers
   - gaussian_depths
   - gaussian_radii
   - accum_weights
   - accum_count
   - accum_blend
   - accum_dist
```

最终 `render(...)` 应该同时服务：

```text
首帧 joint
首帧 skin
后帧 joint
后帧 skin
FastGS densify score
DualGS 原有扩展统计
```

---

## 1.3 修改参数系统

文件：

```text
DualGS/arguments/__init__.py
```

在 `OptimizationParams` 里保留 DualGS 原参数，同时加入 FastGS 参数。

保留 DualGS：

```text
position_lr_init_t1
position_lr_final_t1
position_lr_init_t2
position_lr_final_t2
joint_gs_num
feature_lr
opacity_lr
scaling_lr
rotation_lr
densify_from_iter
densify_until_iter
densification_interval
densify_grad_threshold
```

新增 FastGS：

```text
loss_thresh
grad_abs_thresh
highfeature_lr
lowfeature_lr
grad_thresh
dense
mult
optimizer_type
```

建议不要删 `feature_lr`。  
因为后面 skin / 后帧可能还要兼容原 DualGS 训练配置。

---

## 1.4 修改 GaussianModel：为 FastGS densify/prune 做准备

文件：

```text
DualGS/scene/gaussian_model.py
```

首帧 joint 会用到的修改如下。

---

### 1.4.1 增加 FastGS 状态变量

修改位置：

```text
GaussianModel.__init__
```

新增：

```text
xyz_gradient_accum_abs
tmp_radii
shoptimizer
optimizer_type
```

但保留 DualGS 原有变量：

```text
max_radii2D
xyz_gradient_accum
denom
optimizer
spatial_lr_scale
```

---

### 1.4.2 增加或改造 training_setup_t1

首帧 joint 调用：

```python
joint_gaussian.training_setup_t1(opt)
```

你有两个选择：

```text
方案 A：直接把 training_setup_t1 改成 FastGS 版
方案 B：新增 training_setup_t1_fastgs，再由 train.py 开关选择
```

为了安全，建议方案 B。

需要处理：

```text
1. 初始化 xyz_gradient_accum_abs
2. 把 _features_rest 从主 optimizer 中拆出来
3. 新建 shoptimizer
4. 使用 lowfeature_lr / highfeature_lr
5. 位置学习率仍然用 DualGS 的 position_lr_init_t1 / final_t1
```

注意：  
FastGS 原始代码只有 `position_lr_init`，但 DualGS 分了：

```text
position_lr_init_t1
position_lr_init_t2
```

首帧 joint 必须用 `t1`。

---

### 1.4.3 修改 optimizer 维护函数

文件：

```text
DualGS/scene/gaussian_model.py
```

需要改这些函数：

```text
replace_tensor_to_optimizer
_prune_optimizer
cat_tensors_to_optimizer
prune_points
densification_postfix
```

原因：  
FastGS 多了 `shoptimizer`，而 densify/prune 会改变参数 tensor 的长度。如果只更新 `self.optimizer`，不更新 `self.shoptimizer`，后面一定会出 optimizer state 不匹配。

每个函数要做到：

```text
1. 原 self.optimizer 正常处理
2. 如果 self.shoptimizer 存在，也要同步处理
3. prune 时同步裁剪：
   - xyz_gradient_accum
   - xyz_gradient_accum_abs
   - denom
   - max_radii2D
   - tmp_radii
```

---

### 1.4.4 增加 FastGS densify/prune 方法

从 FastGS 移植到 DualGS 的 `GaussianModel`：

```text
densify_and_split_fastgs
densify_and_clone_fastgs
densify_and_prune_fastgs
final_prune_fastgs
```

但不要删 DualGS 原来的：

```text
densify_and_split
densify_and_clone
densify_and_prune
prune
```

首帧 joint 先用：

```text
densify_and_prune_fastgs
```

后续是否替换 skin / 后帧再分阶段处理。

---

### 1.4.5 修改 add_densification_stats

文件：

```text
DualGS/scene/gaussian_model.py
```

原 DualGS 只统计：

```text
viewspace_point_tensor.grad[:, :2]
```

FastGS 还要统计：

```text
viewspace_point_tensor.grad[:, 2:]
```

所以这里要同时支持：

```text
xyz_gradient_accum
xyz_gradient_accum_abs
denom
```

注意：  
这依赖前面 `screenspace_points = [P, 4]`。  
如果 rasterizer backward 没有返回 `[P, 4]` 梯度，这里会出错。

---

### 1.4.6 增加 FastGS optimizer_step

文件：

```text
DualGS/scene/gaussian_model.py
```

新增：

```text
optimizer_step_fastgs
```

首帧 joint 可以先用 FastGS 原策略：

```text
主 optimizer 每轮更新；
shoptimizer 低频更新；
后期 optimizer 低频更新。
```

但这里先只服务首帧 joint。后帧是否使用这个策略，后面再判断。

---

## 1.5 移植 FastGS metric score 工具

文件：

```text
DualGS/utils/fast_utils.py
```

从 FastGS 复制并适配：

```text
sampling_cameras
get_loss
compute_photometric_loss
normalize
compute_gaussian_score_fastgs
```

需要改的地方：

```text
1. import 从：
   from gaussian_renderer import render_fastgs

   改成：
   from gaussian_renderer import render

2. render(...) 调用要传入：
   - mult
   - get_flag
   - metric_map

3. compute_gaussian_score_fastgs(...) 读取：
   - render_pkg["accum_metric_counts"]

4. fused_ssim 依赖要处理：
   - 可以安装 FastGS 的 fused-ssim
   - 或者暂时改成 DualGS 原 loss_utils.ssim
```

首帧 joint 的 FastGS densify 依赖这个函数。  
如果不移植这个，`densify_and_prune_fastgs` 只能做一半。

---

## 1.6 修改 train.py 的首帧 joint 流程

文件：

```text
DualGS/train.py
```

只看 `training_joint(...)`。

---

### 1.6.1 import 部分

需要新增：

```text
from utils.fast_utils import sampling_cameras, compute_gaussian_score_fastgs
```

如果你保留开关，还要增加对应参数。

---

### 1.6.2 初始化 GaussianModel

当前：

```python
joint_gaussian = GaussianModel(dataset.sh_degree)
```

如果你引入 `optimizer_type`，要改成支持：

```text
GaussianModel(dataset.sh_degree, optimizer_type=opt.optimizer_type)
```

或者在 `GaussianModel.__init__` 里给默认值，保证旧代码不坏。

---

### 1.6.3 training_setup_t1 替换

当前：

```python
joint_gaussian.training_setup_t1(opt)
```

首帧 joint 的 FastGS 版本应改为：

```text
joint_gaussian.training_setup_t1_fastgs(opt)
```

或者通过开关选择：

```text
原 DualGS setup
FastGS setup
```

流程预览阶段建议保留开关，方便回退。

---

### 1.6.4 render 调用

当前：

```python
render_canonical = render(viewpoint_cam, joint_gaussian, pipe, background)
```

合并后仍然调用 `render(...)`，但要支持额外参数：

```text
mult=opt.mult
get_flag=None
metric_map=None
pixel_weights=None
```

首帧 joint 普通训练时：

```text
get_flag=False
metric_map=empty/default
pixel_weights=None
```

在 FastGS score 计算时：

```text
get_flag=True
metric_map=high-error pixel map
```

---

### 1.6.5 densification stats

当前：

```python
joint_gaussian.add_densification_stats(viewspace_point_tensor, visibility_filter)
```

这句保留，但底层 `add_densification_stats` 必须已经支持：

```text
grad[:, :2]
grad[:, 2:]
```

否则 FastGS densify 没有 `grad_abs`。

---

### 1.6.6 densify_and_prune 替换

当前：

```python
joint_gaussian.densify_and_prune(
    opt.densify_grad_threshold,
    0.1,
    scene.cameras_extent,
    size_threshold
)
```

FastGS 版本需要变成：

```text
1. 从 scene.getTrainCameras() 采样若干 camera
2. compute_gaussian_score_fastgs(...)
3. 得到：
   - importance_score
   - pruning_score
4. 调用：
   joint_gaussian.densify_and_prune_fastgs(...)
```

涉及函数：

```text
training_joint
utils/fast_utils.compute_gaussian_score_fastgs
GaussianModel.densify_and_prune_fastgs
```

---

### 1.6.7 opacity reset

当前：

```python
joint_gaussian.reset_opacity()
```

这个要保留。  
但是因为现在可能存在 `shoptimizer`，`reset_opacity()` 里面调用的 `replace_tensor_to_optimizer` 必须支持多 optimizer，否则 opacity 替换后 state 可能不同步。

---

### 1.6.8 joint_gs_num 强制裁剪

当前：

```python
if iteration == opt.densify_until_iter and is_start_frame:
    points_num = joint_gaussian._xyz.shape[0]
    prune_scale = int(points_num // opt.joint_gs_num)
    ...
    joint_gaussian.prune_points(prune_mask)
```

这段要保留。  
但 `prune_points()` 必须支持：

```text
self.optimizer
self.shoptimizer
xyz_gradient_accum_abs
tmp_radii
```

否则裁剪后 optimizer 可能坏。

---

### 1.6.9 optimizer step 替换

当前：

```python
joint_gaussian.optimizer.step()
joint_gaussian.optimizer.zero_grad(set_to_none=True)
```

FastGS 版本：

```text
joint_gaussian.optimizer_step_fastgs(iteration)
```

涉及：

```text
GaussianModel.optimizer_step_fastgs
training_joint
```

首帧 joint 可以先完整使用 FastGS 策略。  
后帧 joint 以后再单独决定是否降频。

---

### 1.6.10 training_report

当前：

```python
training_report(..., scene, render, (pipe, background))
```

如果 `render(...)` 增加了默认参数，那么这里可以不改。

但如果你要求 report 也使用 `mult=opt.mult`，就要把 renderArgs 改成：

```text
(pipe, background, opt.mult)
```

更推荐改 `render` 的默认参数：

```text
mult=0.5
get_flag=None
metric_map=None
pixel_weights=None
```

这样 `training_report` 暂时不动，减少变量。

---

## 1.7 编译和首帧 joint 测试

这一阶段的测试目标不是效果最好，而是验证接口和训练闭环。

建议测试项：

```text
1. rasterizer forward 是否返回完整输出
2. loss.backward() 是否正常
3. viewspace_points.grad 是否是 [P, 4]
4. xyz_gradient_accum_abs 是否正常更新
5. compute_gaussian_score_fastgs 是否能得到 accum_metric_counts
6. densify_and_prune_fastgs 是否能正常 clone/split/prune
7. optimizer_step_fastgs 是否正常
8. point_cloud_xxx.ply 是否正常保存
9. joint_graph.graph_init 是否正常
10. joint_graph.regular_term_setup 是否正常
```

首帧 joint 能跑通后，才进入：

```text
2. 首帧 skin 训练
3. 后帧 joint 训练
4. 后帧 skin 训练
```

---

# 1.8 首帧 joint 阶段需要修改的文件清单

集中列一下，避免遗漏。

## CUDA / C++ rasterizer

```text
DualGS/submodules/diff-gaussian-rasterization/diff_gaussian_rasterization/__init__.py
DualGS/submodules/diff-gaussian-rasterization/rasterize_points.h
DualGS/submodules/diff-gaussian-rasterization/rasterize_points.cu
DualGS/submodules/diff-gaussian-rasterization/ext.cpp
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/auxiliary.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/forward.cu
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.h
DualGS/submodules/diff-gaussian-rasterization/cuda_rasterizer/rasterizer_impl.cu
```

`backward.cu/h` 大概率不用大改，但要检查：

```text
dL_dmeans2D 是否与 [P, 4] 返回兼容
```

---

## Python renderer

```text
DualGS/gaussian_renderer/__init__.py
```

---

## 参数

```text
DualGS/arguments/__init__.py
```

---

## Gaussian model

```text
DualGS/scene/gaussian_model.py
```

重点函数：

```text
__init__
training_setup_t1 / training_setup_t1_fastgs
replace_tensor_to_optimizer
_prune_optimizer
cat_tensors_to_optimizer
prune_points
densification_postfix
densify_and_split_fastgs
densify_and_clone_fastgs
densify_and_prune_fastgs
final_prune_fastgs
add_densification_stats
optimizer_step_fastgs
reset_opacity
```

---

## FastGS 工具函数

```text
DualGS/utils/fast_utils.py
```

---

## 训练入口

```text
DualGS/train.py
```

重点函数：

```text
training_joint
training_report
main parser
```

---

# 这一阶段的核心判断

你现在应该按这个原则做：

```text
首帧 joint 阶段就完成最终 rasterizer 接口合并；
首帧 joint 只是验证这个合并接口能不能训练；
不要用 dummy；
不要暂时绕过 DualGS extra outputs；
不要直接拿 FastGS rasterizer 替换 DualGS rasterizer。
```

这样后面做首帧 skin、后帧 joint、后帧 skin 时，rasterizer 不需要返工，只需要继续接入训练逻辑。