### 1.1.3 矩阵转置

定义：将矩阵的行和列交换。

若：

$A= \begin{bmatrix} 1&2&3\\ 4&5&6 \end{bmatrix}$

则：

$A^T= \begin{bmatrix} 1&4\\ 2&5\\ 3&6 \end{bmatrix}$

常见性质：

- $(A^T)^T=A$
- $(A+B)^T=A^T+B^T$
- $(AB)^T=B^TA^T$
- $(A^{-1})^T=(A^T)^{-1}$

---

### 1.1.4 逆矩阵

- 对于方阵 $A$，若存在矩阵 $A^{-1}$ 满足：

$AA^{-1}=A^{-1}A=I$

则称 $A$ 可逆，$A^{-1}$ 为 $A$ 的逆矩阵。

例如：

$A= \begin{bmatrix} 2&0\\ 0&3 \end{bmatrix}$

则：

$A^{-1}= \begin{bmatrix} \frac12&0\\ 0&\frac13 \end{bmatrix}$

因为：

$AA^{-1}=I$

常见性质：

- $(AB)^{-1}=B^{-1}A^{-1}$
- $(A^{-1})^{-1}=A$
- $(A^T)^{-1}=(A^{-1})^T$

一个 $n\times n$ 方阵可逆的常见等价条件：

- $\det(A)\neq0$
- $\operatorname{rank}(A)=n$
- $0$ 不是 $A$ 的特征值
- $Ax=0$ 只有零解

---

### 1.1.5 行列式

- 行列式只针对**方阵**，结果是一个标量：

$\det(A)$

二维情况下：

$A= \begin{bmatrix} a&b\\ c&d \end{bmatrix}$

则：

$\det(A)=ad-bc$

例如：

$A= \begin{bmatrix} 1&2\\ 3&4 \end{bmatrix}$

有：

$\det(A)=1\times4-2\times3=-2$

常见性质：

- $\det(A)=0$：矩阵不可逆
- $\det(A)\neq0$：矩阵可逆
- $\det(AB)=\det(A)\det(B)$
- $\det(A^T)=\det(A)$
- $\det(A^{-1})=\frac1{\det(A)}$

几何意义：

- $|\det(A)|$：线性变换对面积 / 体积的缩放倍数
- $\det(A)=0$：说明空间被压缩到了更低维

---

### 1.1.6 矩阵的秩

- 秩 $\operatorname{rank}(A)$：矩阵中**线性无关的行 / 列的最大数量**，可以理解为矩阵真正包含的独立维度。

例如：

$A= \begin{bmatrix} 1&2\\ 2&4 \end{bmatrix}$

第二行是第一行的 2 倍，因此只有一个独立方向：

$\operatorname{rank}(A)=1$

常见性质：

- $\operatorname{rank}(A)=\operatorname{rank}(A^T)$
- $\operatorname{rank}(AB)\le\min(\operatorname{rank}(A),\operatorname{rank}(B))$
- 对 $n\times n$ 方阵：

$\operatorname{rank}(A)=n\iff A\text{ 可逆}$

- 若：

$\operatorname{rank}(A)<n$

则矩阵不满秩，不可逆。

---

## 1.2 矩阵性质与特殊矩阵

### 1.2.1 trace 与 determinant

#### 1）trace

- trace 为方阵主对角线元素之和：

$\operatorname{tr}(A)=\sum_i A_{ii}$

例如：

$A= \begin{bmatrix} 1&2\\ 3&4 \end{bmatrix}$

则：

$\operatorname{tr}(A)=1+4=5$

常见性质：

- $\operatorname{tr}(A+B)=\operatorname{tr}(A)+\operatorname{tr}(B)$
- $\operatorname{tr}(AB)=\operatorname{tr}(BA)$
- $\operatorname{tr}(A)=\operatorname{tr}(A^T)$

#### 2）determinant

- determinant 就是前面的行列式：

$\det(A)$

后面结合特征值需要记：

$\operatorname{tr}(A)=\sum_i\lambda_i$$\det(A)=\prod_i\lambda_i$

即：

- trace = 所有特征值之和
- determinant = 所有特征值之积

---

### 1.2.2 对称矩阵、正交矩阵

#### 1）对称矩阵

定义：

$A^T=A$

例如：

$A= \begin{bmatrix} 1&2\\ 2&3 \end{bmatrix}$

重要性质：

- 特征值均为实数
- 不同特征值对应的特征向量互相正交
- 一定可以正交对角化：

$A=Q\Lambda Q^T$

---

#### 2）正交矩阵

定义：

$Q^TQ=I$

因此：

$Q^{-1}=Q^T$

性质：

- 保持向量长度：

$\|Qx\|_2=\|x\|_2$

- 保持两个向量之间的夹角
- 行向量和列向量均为单位正交向量

因此正交矩阵可以理解为：

> 只进行旋转 / 镜像，不改变空间尺度。

---

### 1.2.3 幂等矩阵、可交换矩阵

#### 1）幂等矩阵

定义：

$A^2=A$

重要性质：

- 任意正整数 $k$：

$A^k=A$

- 特征值只能为：

$\lambda\in\{0,1\}$

- 如果 $A$ 可逆：

$A=I$

- 幂等矩阵可以对角化
- trace 等于 rank：

$\operatorname{tr}(A)=\operatorname{rank}(A)$

因为特征值只有 0 和 1。

---

#### 2）可交换矩阵

若：

$AB=BA$

称 $A,B$ 可交换。

一般情况下：

$AB\neq BA$

但如果 $AB=BA$，很多标量公式才可以直接使用。

例如：

$(A+B)^2=A^2+AB+BA+B^2$

若：

$AB=BA$

则：

$(A+B)^2=A^2+2AB+B^2$

另外：

$AB=BA\Rightarrow AB^k=B^kA$

若 $A$ 可逆：

$AB=BA\Rightarrow A^{-1}B=BA^{-1}$

---

### 1.2.4 正定矩阵、半正定矩阵

这里主要讨论**实对称矩阵**。

#### 1）正定矩阵

若对于任意非零向量 $x$：

$x^TAx>0$

则：

$A\succ0$

称 $A$ 为正定矩阵。

对于实对称矩阵：

$A\succ0\iff\lambda_i>0,\quad\forall i$

即所有特征值都大于 0。

---

#### 2）半正定矩阵

若：

$x^TAx\ge0$

则：

$A\succeq0$

称为半正定矩阵。

对于实对称矩阵：

$A\succeq0\iff\lambda_i\ge0,\quad\forall i$

注意：

- 正定：所有特征值 $>0$
- 半正定：允许存在特征值 $=0$

这个知识点后面判断**凸函数的 Hessian**会直接用到。

---

### 1.2.5 二次型

形式：

$f(x)=x^TAx$

称为二次型。

例如：

$A= \begin{bmatrix} 2&0\\ 0&1 \end{bmatrix},\quad x= \begin{bmatrix} x_1\\ x_2 \end{bmatrix}$

则：

$x^TAx=2x_1^2+x_2^2$

因此二次型与矩阵的正定性直接相关：

- $A\succ0$：$x^TAx>0$
- $A\succeq0$：$x^TAx\ge0$

优化问题中经常出现：

$f(x)=\frac12x^TAx+b^Tx+c$

其 Hessian 就是：

$\nabla^2f(x)=A$

所以：

$A\succeq0\Rightarrow f(x)\text{ 是凸函数}$

---

## 1.3 特征分析与矩阵分解

### 1.3.1 特征值、特征向量

定义：

$Av=\lambda v$

其中：

- $v$：特征向量
- $\lambda$：对应特征值

含义：

> 矩阵 $A$ 作用到特征向量 $v$ 后，方向不变，只缩放了 $\lambda$ 倍。

例如：

$A= \begin{bmatrix} 2&0\\ 0&3 \end{bmatrix}$

有：

$A \begin{bmatrix} 1\\ 0 \end{bmatrix} = 2 \begin{bmatrix} 1\\ 0 \end{bmatrix}$

所以：

$v= \begin{bmatrix} 1\\ 0 \end{bmatrix}$

是特征向量，对应：

$\lambda=2$

特征值由：

$\det(A-\lambda I)=0$

求出。

---

### 1.3.2 特征值与矩阵性质

需要重点记：

$\operatorname{tr}(A)=\sum_i\lambda_i$$\det(A)=\prod_i\lambda_i$

另外：

$A\text{ 可逆}\iff0\text{ 不是 }A\text{ 的特征值}$

因为：

$\det(A)=\prod_i\lambda_i$

只要有一个：

$\lambda_i=0$

就有：

$\det(A)=0$

矩阵不可逆。

---

### 1.3.3 特征值分解

如果 $A$ 有足够多线性无关的特征向量，则：

$A=P\Lambda P^{-1}$

其中：

- $P$：每列为一个特征向量
- $\Lambda$：对角线上为对应特征值

如果 $A$ 为实对称矩阵：

$A=Q\Lambda Q^T$

因为此时特征向量可以组成正交矩阵：

$Q^{-1}=Q^T$

用途：

- 分析线性变换
- PCA
- 快速计算矩阵高次幂

例如：

$A^k=P\Lambda^kP^{-1}$

而 $\Lambda$ 是对角矩阵，因此高次幂很好计算。

---

### 1.3.4 SVD

任意矩阵：

$A\in\mathbb R^{m\times n}$

都可以进行奇异值分解：

$A=U\Sigma V^T$

其中：

- $U$：左奇异向量
- $V$：右奇异向量
- $\Sigma$：奇异值组成的对角矩阵

注意与特征值分解的区别：

- 特征值分解主要针对**方阵且要求可对角化**
- SVD 对**任意矩阵**都存在

奇异值满足：

$\sigma_i=\sqrt{\lambda_i(A^TA)}$

常见用途：

- 降维
- 低秩近似
- 数据压缩
- PCA
- 伪逆

---

## 1.4 典型应用

### 1.4.1 线性方程组

考虑：

$Ax=b$

若 $A$ 为可逆方阵：

$x=A^{-1}b$

但实际数值计算中通常**不会真的显式计算逆矩阵**，而是通过矩阵分解等方法求解。

如果：

$\operatorname{rank}(A)<n$

可能出现：

- 无解
- 无穷多解

---

### 1.4.2 最小二乘

当：

$Ax=b$

没有精确解时，可以寻找一个 $x$，使误差最小：

$\min_x\|Ax-b\|_2^2$

对 $x$ 求梯度：

$\nabla_x\|Ax-b\|_2^2=2A^T(Ax-b)$

令：

$2A^T(Ax-b)=0$

得到正规方程：

$A^TAx=A^Tb$

如果 $A^TA$ 可逆：

$x=(A^TA)^{-1}A^Tb$

本质：

> 找到一个 $Ax$，使其与 $b$ 的欧氏距离最小。

线性回归就是典型最小二乘问题。

---

### 1.4.3 PCA

PCA：Principal Component Analysis，主成分分析。

核心目标：

> 将高维数据投影到低维空间，同时尽可能保留数据的方差 / 信息。

假设每个样本有 $d$ 个特征：

$X\in\mathbb R^{N\times d}$

首先进行中心化，然后计算协方差矩阵：

$C=\frac1N X^TX$

其维度为：

$C\in\mathbb R^{d\times d}$

然后对协方差矩阵做特征值分解：

$C=Q\Lambda Q^T$

- 最大特征值对应的特征向量：数据方差最大的方向
- 第二大特征值对应：第二大方差方向
- 依次类推

如果降到 $k$ 维，就选择最大的 $k$ 个特征值对应的特征向量。

因此 PCA 可以理解为：

> **寻找数据方差最大的几个正交方向，然后把数据投影到这些方向上。**

常见笔试结论：

- 原始特征维数为 $d$，协方差矩阵维度为：

$d\times d$

- 主成分之间相互正交
- 特征值越大，对应方向解释的数据方差越大
- PCA 是无监督降维方法